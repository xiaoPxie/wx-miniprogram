.tabs {}
.tabs_title {
  display: flex;
  position: sticky;
  top: 0;
  text-align: center;
  background: #fff;
}
.title_item {
  flex: 1;
  padding: 15rpx 0;
  border-bottom: 0.5rpx solid #ccc;
}
.active {
  color: var(--themeColor);
  border-bottom: 0.5rpx solid currentColor;
}
.tabs_content {

}